<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.recipe.dao.ChatbotaiDAO">

    <!-- 자주 묻는 질문 -->
    <select id="selectPopularQuestions" resultType="ChatbotaiDTO">

        SELECT
            chat_id,
            category,
            question,
            answer,
            keywords,
            link_url,
            link_text,
            use_yn,
            created_at,
            updated_at,
            sort_order
        FROM chatbotai
        WHERE use_yn = 'Y'
        ORDER BY sort_order ASC
        LIMIT 5

    </select>


    <!-- 사용자 질문 관련 정보 검색 -->
    <select id="searchChatbot" resultType="ChatbotaiDTO">

        SELECT
            chat_id,
            category,
            question,
            answer,
            keywords,
            link_url,
            link_text,
            use_yn,
            created_at,
            updated_at,
            sort_order
        FROM chatbotai
        WHERE use_yn = 'Y'
        AND (
            <foreach collection="list"
                    item="keyword"
                    separator=" OR ">

                question LIKE CONCAT('%', #{keyword}, '%')
                OR keywords LIKE CONCAT('%', #{keyword}, '%')

            </foreach>
        )
        ORDER BY sort_order ASC

    </select>

</mapper>